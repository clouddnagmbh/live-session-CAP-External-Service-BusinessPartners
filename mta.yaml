_schema-version: 3.3.0
ID: business-partners
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: business-partners-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: business-partners-auth
      - name: business-partners-db
      - name: business-partners-destination

  - name: business-partners-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: business-partners-db

  - name: business-partners-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: businesspartnersexample
          artifacts:
            - businesspartnersexample.zip
          target-path: app/
    requires:
      - name: business-partners-auth
      - name: srv-api
      - name: business-partners-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true

  - name: businesspartnersexample
    type: html5
    path: app/business-partners-example
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  - name: business-partners-destinations
    type: com.sap.application.content
    requires:
      - name: business-partners-auth
        parameters:
          service-key:
            name: business-partners-auth-key
      - name: business-partners-html5-repo-host
        parameters:
          service-key:
            name: business-partners-html5-repo-host-key
      - name: srv-api
      - name: business-partners-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: business-partners-html5-repository
              ServiceInstanceName: business-partners-html5-repo-host
              ServiceKeyName: business-partners-html5-repo-host-key
              sap.cloud.service: businesspartners.service
            - Name: business-partners-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: business-partners-auth
              ServiceKeyName: business-partners-auth-key
              sap.cloud.service: businesspartners.service

resources:
  - name: business-partners-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: business-partners-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
  - name: business-partners-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: business-partners-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true

    requires:
      - name: srv-api

  - name: business-partners-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
